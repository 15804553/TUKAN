<Window x:Class="SKRYBEK.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:SKRYBEK.App.Views.Chrome"
        Title="SKRYBEK — Ustawienia"
        Icon="pack://application:,,,/SKRYBEK;component/Resources/skrybek.ico"
        Width="860" Height="680"
        MinWidth="700" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <chrome:SkrybekTitleBar Grid.Row="0"
                                     Title="SKRYBEK — Ustawienia"
                                     ShowMaximizeButton="False" />

            <ContentControl x:Name="SettingsHost" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
