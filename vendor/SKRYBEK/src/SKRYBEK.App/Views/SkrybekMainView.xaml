<UserControl x:Class="SKRYBEK.App.Views.SkrybekMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:SKRYBEK.App.Views"
             xmlns:vm="clr-namespace:SKRYBEK.App.ViewModels">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="＋  Nowy rozkaz"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding NowyRozkazCommand}"
                            IsEnabled="{Binding CanEdit}"
                            Margin="0,0,8,0" />
                    <ComboBox x:Name="RokCombo"
                              IsEditable="True"
                              Style="{StaticResource DarkComboBox}"
                              Width="80"
                              SelectedItem="{Binding AktualnyRok}"
                              SelectionChanged="RokCombo_SelectionChanged"
                              Margin="0,0,8,0" />
                </StackPanel>

                <StackPanel x:Name="SessionToolbarPanel"
                            Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock x:Name="UserLabel"
                               Foreground="{StaticResource ForegroundBrush}"
                               VerticalAlignment="Center"
                               FontSize="12"
                               Margin="0,0,16,0" />
                    <Button x:Name="SettingsButton"
                            Content="Ustawienia"
                            Style="{StaticResource SecondaryButton}"
                            Click="Settings_Click"
                            Visibility="{Binding CanOpenSettings, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="0,0,8,0" />
                    <Button x:Name="LogoutButton"
                            Content="Wyloguj"
                            Style="{StaticResource SecondaryButton}"
                            Click="Logout_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200" MaxWidth="380" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="ROZKAZY"
                               Style="{StaticResource SectionHeader}"
                               Margin="12,10,12,6" />

                    <ListBox Grid.Row="1"
                             x:Name="RozkazyList"
                             ItemsSource="{Binding Rozkazy}"
                             SelectedItem="{Binding WybranyRozkaz}"
                             Background="Transparent"
                             BorderThickness="0"
                             SelectionChanged="RozkazyList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="Bold" FontSize="13"
                                                   Foreground="{StaticResource ForegroundBrush}">
                                            <Run Text="Nr " /><Run Text="{Binding NumerFormatowany, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock FontSize="12"
                                                   Foreground="{StaticResource ForegroundMutedBrush}"
                                                   Text="{Binding DataFormatowana, Mode=OneWay}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="🔒"
                                                   FontSize="12"
                                                   Margin="0,0,4,0"
                                                   VerticalAlignment="Center"
                                                   ToolTip="Edycja zablokowana przez DCA JRG"
                                                   Visibility="{Binding CzyZablokowany,
                                                                Converter={StaticResource BoolToVisibilityConverter}}" />
                                        <Button Content="✕"
                                                Style="{StaticResource ChromeIconButton}"
                                                Width="22" Height="22" FontSize="10"
                                                Visibility="{Binding DataContext.CanUsunRozkaz,
                                                             RelativeSource={RelativeSource AncestorType=UserControl},
                                                             Converter={StaticResource BoolToVisibilityConverter}}"
                                                Tag="{Binding}"
                                                Click="DeleteRozkaz_Click" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          Background="{StaticResource BorderBrush}"
                          ResizeBehavior="PreviousAndNext" />

            <Border Grid.Column="2" Background="{StaticResource BackgroundBrush}">
                <views:RozkazEditorView x:Name="RozkazEditor"
                                        DataContext="{Binding EditorVm}"
                                        Visibility="{Binding EditorVm, Converter={StaticResource NullToVisibilityConverter}}" />
            </Border>
        </Grid>

        <Border x:Name="StatusBarHost"
                Grid.Row="2"
                Background="{StaticResource SurfaceVariantBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,4"
                Visibility="{Binding ShowStatusBar, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <TextBlock x:Name="StatusText"
                           Text="{Binding StatusMessage}"
                           Foreground="{StaticResource ForegroundMutedBrush}"
                           FontSize="11"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center" />
                <ProgressBar IsIndeterminate="True"
                             Height="3"
                             Width="120"
                             HorizontalAlignment="Right"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
