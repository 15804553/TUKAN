<Window x:Class="SKRYBEK.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:SKRYBEK.App.Views.Chrome"
        xmlns:local="clr-namespace:SKRYBEK.App.Views"
        Title="SKRYBEK"
        Icon="pack://application:,,,/SKRYBEK;component/Resources/skrybek.ico"
        Height="900" Width="1400"
        MinHeight="650" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <chrome:SkrybekTitleBar Grid.Row="0"
                                     Title="SKRYBEK — Rozkaz Dzienny JRG"
                                     ShowMinimizeButton="True"
                                     ShowMaximizeButton="True" />

            <local:SkrybekMainView x:Name="MainView" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
