<Window x:Class="SKRYBEK.App.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:SKRYBEK.App.Views.Chrome"
        Title="Zmień hasło"
        Icon="pack://application:,,,/SKRYBEK;component/Resources/skrybek.ico"
        Width="360" Height="240"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <chrome:SkrybekTitleBar Grid.Row="0" Title="Zmień hasło"
                                     ShowMinimizeButton="False" ShowMaximizeButton="False" />
            <StackPanel Grid.Row="1" Margin="24,16">
                <TextBlock Text="Nowe hasło" Style="{StaticResource FormLabel}" Margin="0,0,0,4" />
                <PasswordBox x:Name="Haslo1" Style="{StaticResource DarkPasswordBox}" Margin="0,0,0,8" />
                <TextBlock Text="Powtórz hasło" Style="{StaticResource FormLabel}" Margin="0,0,0,4" />
                <PasswordBox x:Name="Haslo2" Style="{StaticResource DarkPasswordBox}" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Anuluj" Style="{StaticResource SecondaryButton}" Click="Anuluj_Click" Margin="0,0,8,0" />
                    <Button Content="Zmień" Style="{StaticResource PrimaryButton}" Click="Zmien_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
