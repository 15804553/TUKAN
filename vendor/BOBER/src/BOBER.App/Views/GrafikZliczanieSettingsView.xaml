<UserControl x:Class="BOBER.App.Views.GrafikZliczanieSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="6"
            Padding="14,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Konfiguracja dotyczy wyłącznie bieżącej zmiany. „Wolne miejsca” jest stałe. W grupie pozycje łączy LUB, grupy łączy ORAZ."
                       TextWrapping="Wrap"
                       Foreground="{StaticResource ForegroundBrush}"
                       Margin="0,0,0,10" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Dodaj" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="0,0,8,0"
                        Click="OnAddClick" />
                <Button Content="Edytuj" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="0,0,8,0"
                        Click="OnEditClick" />
                <Button Content="Usuń" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="0,0,8,0"
                        Click="OnRemoveClick" />
                <Button Content="W górę" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="0,0,8,0"
                        Click="OnMoveUpClick" />
                <Button Content="W dół" Style="{StaticResource SecondaryButton}" Padding="12,6"
                        Click="OnMoveDownClick" />
            </StackPanel>

            <DataGrid x:Name="WierszeGrid"
                      Grid.Row="2"
                      MinHeight="280"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      Background="{StaticResource SurfaceBrush}"
                      RowBackground="{StaticResource SurfaceBrush}"
                      AlternatingRowBackground="{StaticResource BackgroundBrush}"
                      Foreground="{StaticResource ForegroundBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1"
                      HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" Width="160" />
                    <DataGridTextColumn Header="Typ" Binding="{Binding TypTekst}" Width="100" />
                    <DataGridTextColumn Header="Reguła" Binding="{Binding Skrot}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Zapisz" Style="{StaticResource PrimaryButton}" Padding="16,8"
                        Click="OnSaveClick" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
