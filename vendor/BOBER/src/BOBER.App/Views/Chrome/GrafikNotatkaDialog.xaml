<Window x:Class="BOBER.App.Views.Chrome.GrafikNotatkaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notatka"
        Width="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.6" Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Belka tytułowa: tytuł + ✓ (OK) + ✕ (Anuluj) -->
            <Border Background="{StaticResource TitleBarBrush}"
                    MouseLeftButtonDown="OnTitleBarMouseDown"
                    CornerRadius="8,8,0,0">
                <Grid Height="36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="TitleTextBlock"
                               Text="Notatka"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource TitleBarForegroundBrush}"
                               FontWeight="SemiBold"
                               FontSize="13" />

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="✓"
                                Style="{StaticResource ChromeIconButton}"
                                ToolTip="Zatwierdź"
                                FontWeight="Bold"
                                Click="OnAcceptClick" />
                        <Button Content="✕"
                                Style="{StaticResource ChromeCloseButton}"
                                ToolTip="Anuluj"
                                Click="OnCancelClick" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Padding="16">
                <TextBox x:Name="NoteTextBox"
                         MinHeight="100"
                         MaxHeight="220"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="13"
                         Foreground="{StaticResource ForegroundBrush}"
                         Background="{StaticResource BackgroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1"
                         Padding="8"
                         CaretBrush="{StaticResource ForegroundBrush}" />
            </Border>
        </Grid>
    </Border>
</Window>
