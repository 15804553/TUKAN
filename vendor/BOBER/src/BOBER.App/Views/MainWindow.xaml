<Window x:Class="BOBER.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:BOBER.App.Views.Chrome"
        xmlns:local="clr-namespace:BOBER.App.Views"
        Title="BOBER"
        Icon="/Resources/bober.ico"
        Height="930"
        Width="1280"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterScreen">
    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.7" Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <chrome:BoberTitleBar x:Name="TitleBar"
                                    Title="BOBER"
                                    ShowMinimizeButton="True"
                                    ShowMaximizeButton="True" />

            <local:BoberGrafikView x:Name="GrafikView" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
