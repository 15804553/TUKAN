<Window x:Class="BOBER.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:BOBER.App.Views.Chrome"
        Title="BOBER — Ustawienia"
        Icon="/Resources/bober.ico"
        Height="700"
        Width="860"
        MinHeight="600"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.6" Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <chrome:BoberTitleBar Title="BOBER — Ustawienia"
                                    ShowMinimizeButton="False"
                                    ShowMaximizeButton="False" />

            <ContentControl x:Name="SettingsHost" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
