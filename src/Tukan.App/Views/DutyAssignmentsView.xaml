<UserControl x:Class="Tukan.App.Views.DutyAssignmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock x:Name="ShiftTextBlock"
                               Foreground="{StaticResource ForegroundMutedBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0" />
                    <TextBlock x:Name="YearTextBlock"
                               Foreground="{StaticResource ForegroundMutedBrush}"
                               FontSize="12"
                               VerticalAlignment="Center" />
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Odśwież"
                        Style="{StaticResource SecondaryButton}"
                        Padding="12,6"
                        Click="OnRefreshClick" />
            </Grid>
        </Border>

        <TabControl x:Name="MonthTabControl"
                    Grid.Row="1"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0"
                    Margin="8,8,8,0"
                    SelectionChanged="OnMonthTabChanged">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem"
                       BasedOn="{StaticResource BoberTabItem}" />
            </TabControl.ItemContainerStyle>
        </TabControl>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,5">
            <TextBlock Foreground="{StaticResource ForegroundMutedBrush}"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Text="Skróty: DZ — dowódca zmiany | PA — punkt alarmowy | SZ — szef zmiany | GA — garażomistrz | KPP — dowódca działań SGRW-N | BO — bosman | SO — sonarzysta | PD — podoficer dyżurny | SD — strażak dyżurny" />
        </Border>
    </Grid>
</UserControl>
